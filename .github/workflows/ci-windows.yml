# based on: https://github.com/actions/starter-workflows/blob/main/ci/cmake-multi-platform.yml
# https://docs.github.com/actions/learn-github-actions/managing-complex-workflows#using-a-build-matrix

name: CI Windows

on: [push]

jobs:
  build:
    runs-on: windows-latest

    strategy:
      matrix:
        preset: [vs2022-x86]
        config: [Debug, Release]

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: ${{ contains(matrix.preset, 'x86') && 'x86' || 'x64' }}

      - name: Configure CMake
        run: cmake --preset ${{ matrix.preset }} "-DSOURCE_DIST_PROJECT_VERSION=0.0.0"

      - name: Build
        run: cmake --build build/${{ matrix.preset }} --config ${{ matrix.config }} --parallel

      - name: Test
        run: ctest --test-dir build/${{ matrix.preset }} --build-config ${{ matrix.config }} --output-on-failure --verbose --output-junit TEST-${{ matrix.preset }}-${{ matrix.config }}.xml

      - name: Package
        run: cpack --config build/${{ matrix.preset }}/CPackSourceConfig.cmake

      - name: JUnit Report
        uses: mikepenz/action-junit-report@v4
        if: success() || failure()
        with:
          report_paths: build/**/TEST-*.xml

      - name: Upload Distributable Packages
        uses: actions/upload-artifact@v5
        with:
          name: source-package-${{ matrix.preset }}-${{ matrix.config }}
          path: dist/_CPack_Packages/win32-Source/ZIP/MKLogging-0.0.0-Source
